# SD-300 v3 Native GUI

## Durable context

- Repository: `C:\Users\hey\git\qube-system-diagnostics`.
- **v3.0.0 merged to `main` 2026-07-22 (PR #4, merge `4e17c41`).** Working
  branch `codex/sd300-v3-native-gui` is retired; new work branches from `main`.
- **Read `docs/agents/2026-07-22-codex-loop-post-mortem.md` before a long
  session here** — it records why the effort stalled 24+ hours and the rules
  that closed it (land validated fixes immediately; fix silent reporting before
  guessing; renegotiate expensive gates with the operator; prove
  runtime-specific behavior on that runtime; eliminate observers before
  measuring; bound tool retries — Computer Use cannot see the Windows tray).
- The dual-frontend editing model (what the TUI and GUI share, what stays
  separate, and the recipe for wiring a field into both) lives in `CLAUDE.md`
  and `AGENTS.md`; ADRs 0001–0003 are the worked evidence examples.
- Product contract: keep bare `sd300` and every v2 CLI/TUI behavior unchanged; add only `sd300 gui`; installers own CLI plus GUI as one composite product.
- GUI stack: Vercel Native SDK 0.5.4, Zig 0.16.0, Rust 1.95, exact distribution locks, baseline x86-64 CPU, no local `.path` dependencies.
- Supported v3 contract remains six targets: Windows x86-64, macOS x86-64/ARM64, Linux GNU x86-64/ARM64, Linux musl x86-64.
- Update preserves proven install origin. Existing Cargo v2 users intentionally require two same-version updates: first update installs the v3 CLI; second transfers to the composite managed product.
- Public release order is candidate branch -> unchanged reviewed `main` -> fresh immutable tag -> atomic assets/crate/latest -> fresh-public-byte and physical verification. Never retag or replace released bytes.

## Current release blockers

- Reproduce and eliminate the operator-observed severe scrolling/input lag that appears on scrollable pages after the end-user app has been open for roughly one minute. A noninteractive CPU/memory soak does not prove this gate.
- Determine why the exact final GUI exited cleanly after about 25 minutes of the two-hour Processes soak, then run a replacement soak and retain explicit frame/input evidence. The failed attempt left an empty JSON and stderr at `target/performance-final-soak-20260721-233810.stderr.log`.
- Review, test, commit, push, and rerun the two-file managed-uninstall cleanup fix in `src/update.rs` and `scripts/validate-windows-self-update.ps1`.
- Supply Makira App/Game embedding-license evidence or explicitly authorize replacement with a distributable open font. Possession of the font bytes or CI secrets is not license evidence.
- Run final signed/notarized package qualification, attest exact subjects, and verify immutable public bytes only after the preceding gates pass.

## Evidence boundaries

- Exact-head ordinary CI and cross-platform Native GUI builds are green at `8aaeef7b1b3d009ba5d18f0e14e6cf57d20a09d0`.
- Physical Alienware GUI, tray/startup/singleton, Cargo rollback/commit, Corporate MSI takeover/repair/uninstall, and v2 restoration are proven locally.
- Hosted macOS package preflight is not notarized physical Mac acceptance.
- The Native SDK canvas has an accessible name but does not expose its internal widget tree to Windows/Linux screen readers; the unchanged TUI is the documented fallback.

## Reference products

- Honk300 v1.3.4 in `C:\Users\hey\git\goose` is the closest installer/updater comparator: receipt-authoritative ownership, immutable release slots, stable selectors, exact-tag manifest/hash verification, deferred cleanup, candidate -> same-SHA main -> immutable tag -> atomic publication -> fresh-public-byte proof.
- TR-300 and ND-300 remain secondary lifecycle references. Reuse invariants, not product-specific paths or ownership assumptions.
