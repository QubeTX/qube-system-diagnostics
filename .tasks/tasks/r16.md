TT;DR: Restore the original frame and input targets after v4 using retained native measurements. The operator accepted a version-scoped responsiveness limit for 4.0.0 on all platforms.

## Why

The operator authorized 100 ms responsiveness on 2026-09-23 while requiring the macOS duplicate-event and accessibility defects to be fixed before v4. ADR 0016 records the exact scope; ADR 0017 later records a separate v4-only resource decision, tracked by #r17. All original goals are preserved in docs/next-version-targets.md.

## Scope

Profile and improve native frame work and input delivery across all six targets. Preserve measurement cadence, actual capture times, accessibility behavior and bounded resource use. No cosmetic benchmark exclusions or silent changes to the measured interval.

## Plan

Use the final v4 artifact hashes and native interaction reports as the baseline. Investigate one attributed cause per cycle, validate behavior on the affected runtime, and compare the unchanged method before and after. The v4-only policy expires automatically for the next version.

## Acceptance

Every native cohort meets frame-work p95 at most 16.7 ms and input p95 at most 50 ms, with ordinary refresh at most 100 ms and functional checks still passing.

## Verification

- [ ] Retain final v4 source/artifact identities and original-target results
- [ ] Attribute costs and qualify focused fixes on native runtimes
- [ ] Pass original timing limits on every supported target

## Status

BACKLOG, owner Codex. Deliberately outside the 4.0.0 responsiveness release bar; resource follow-up is tracked by #r17, and correctness requirements remain unchanged.

## Activity

- 2026-09-24 — codex: operator explicitly accepts the observed 116.583 ms Intel refresh for 4.0.1 (ADR 0020). Preserve the original failed verdict and timing data; future ordinary-refresh target remains at most 100 ms, with original frame/input p95 targets unchanged. This releases the specific performance blocker in #p4h without claiming a renderer fix.

- 2026-09-24 — codex: merged-commit CI 36050405161 later records a failed compact User-mode Intel refresh maximum of 116.583 ms (cohort frame p95 20.495 ms). Input p95 61.758 ms and overall frame p95 52.100 ms pass; other five platforms pass. Publication max 15.775 ms, layout max 32.007 ms and present max 52.503 ms do not alone attribute the exact critical path. Preserve native-interaction-published-ae5a8995.json; do not label this the earlier publication stall, noise, a waiver or a passing gate. Public 4.0.1 is immutable; ADR 0019 closes the separate publishing dependency gap. Further rendering attribution remains required.

- 2026-09-24 — codex: the observer-ordering correction passes all current release gates in native CI 36045921364, retained in native-interaction-latency-08b7fe5.json. Input p95 spans 16.107–79.142 ms and frame p95 spans 11.856–60.162 ms. Preserve Intel's 121.092 ms individual navigation response for attribution; it is not another publication stall. Original frame/input goals remain unsatisfied on some platforms and this task stays in backlog. No timing threshold or measurement interval changed.

- 2026-09-24 — codex: the operator selected fixing the current 100 ms overruns before the corrective release. #p4h owns that immediate lane; this task retains the stricter future targets. Add native diagnostic splits before changing scheduling, preserving the original interval, full input counts and all historical failed results.

- 2026-09-24 — codex: final installer candidate efbab9e keeps GUI/monitoring source unchanged from d18a4e5. Native CI 36037699515 records Intel input p95 104.133 ms and musl keyboard p95 498.065 ms; other four native targets pass the current release limit. Musl has six consecutive key-response delays (115.605–666.377 ms) with exactly one completion each and affected synchronous frame work below 10.488 ms. Retain source/artifact IDs and bounded delayed-input data in docs/qualification/v4/native-interaction-discovery-efbab9e.json. This supports a real event-to-present delay, but not attribution to GTK scheduling, collector contention or runner load. No third blind repeat. Release-policy decision remains in #p4h; original next-version targets remain unchanged.

- 2026-09-23 — codex: corrective candidate d8a6f87 preserves eight first/repeat native reports in docs/qualification/v4/native-interaction-updater-d8a6f87.json. GNU x86-64 input timing fails twice, reaching 875.864 ms navigation p95 and 425.557 ms keyboard p95 on the repeat, while frame work/refresh and exact input completion checks pass. Other five targets pass on their first run or the one permitted fresh-runner repeat. The prior GNU smoke leaves no owned processes. Investigate event scheduling, observer publication and native/automation clock domains with attributed runtime evidence; do not treat these hypotheses as a root cause or silently discard the failed readings. Release decision remains in #p4h.

- 2026-09-23 — codex: ADR 0018 records the operator-authorized 4.0.1 updater correction with the same v4 ceilings. Original targets now resume immediately after that corrective patch; no numeric limit was raised and all original measurements remain intact.

- 2026-09-23 — codex: recorded the operator's all-platform v4 responsiveness decision in ADR 0016 and added expiring policy fixtures while preserving original-target verdicts.
