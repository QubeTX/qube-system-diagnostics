TT;DR: Qualify and publish v3.0.0 only after the complete GUI and lifecycle matrices pass on all six existing targets and the physical Windows candidate is approved. Probe Native SDK's additional Windows ARM64 and Linux-musl ARM64 toolchains separately; do not advertise them until their complete SD-300 lifecycle is owned and qualified.

## Why
Direct operator order and release closure for the milestone. A green build on one host is not enough for a cross-platform monitoring product.

## Plan
Run Rust/Native SDK quality gates, all hosted native matrices, physical Alienware installer and performance proof, SBOM/provenance generation, immutable release qualification, public-byte verification, Cargo two-update messaging, and website documentation checks.

## Impact
Publishes the new major release to current users. Partial publication or inaccurate platform claims are unacceptable.

## Acceptance
All six existing target and lifecycle gates pass, the public release and crate are consistent, and the website documents install/update/uninstall and verification correctly. Any additional architecture is advertised only after the same installer/update/uninstall and runtime proof.

## Verification
- [ ] Hosted and physical qualification matrices pass with archived evidence
- [ ] Public crate, release assets, checksums, attestations, stable routers, and website copy agree on v3.0.0
- [ ] Fresh install, update, repair, GUI/TUI launch, and uninstall pass against public bytes

## Status
Queued behind #gux and #cpl. The feature branch has pushed product head `8aaeef7b1b3d009ba5d18f0e14e6cf57d20a09d0`, two local documentation/handoff commits above it, and an uncommitted two-file Windows cleanup correction. Exact-head ordinary CI `29892152552`, release planning `29892152578`, and macOS preflight `29892224525` are green; Windows installer run `29892216141` failed on the known pre-fix managed-uninstall prompt. Release remains blocked by the operator-reported minute-old scroll lag, the prematurely exited/empty two-hour soak and frame/input evidence, an exact-head Windows installer replay after review of the cleanup correction, Makira app-embedding license evidence or an authorized open-font replacement, signed/notarized candidate qualification, provenance, immutable publication, and fresh public-byte/site/physical acceptance. No merge, tag, crate publication, or release is authorized by the current evidence.

## Activity
- 2026-07-20 21:26 — created from the approved v3 implementation plan (agent: codex)
- 2026-07-21 07:35 — coordinated all 18 product-version surfaces at 3.0.0, added exact Native SDK/Zig/Rust toolchain records and clean/offline dependency checks, expanded pull-request GUI builds across the five native host lanes plus pinned Alpine musl, and made final qualification fail closed unless source checkout, tag, draft target, artifact checksums, SBOM, and attestations all identify the same candidate. Local workflow structure, distribution pins, path-leak checks, Rust fmt/Clippy, 71 unit tests, 2 CLI compatibility tests, and 7 engine ABI tests pass; hosted and sustained-performance gates remain open (agent: codex)
- 2026-07-21 08:31 — hardened release qualification so an already-public rerun verifies immutable metadata instead of overwriting it, stable post-public installers are checksum-verified before execution, every manifest subject is provenance-verified, explicit tags and existing tags must resolve to the exact Cargo version/source SHA, and all host lanes prove clean-cache plus warmed/offline Native SDK restoration. The local cargo-dist plan contains all six required targets and no generic CLI-only MSI; hosted signing, GTK-free launch, native lifecycle, performance-soak, attestation, and public-byte proof remain open (agent: codex)
- 2026-07-21 18:31 — after 15 hours, performed a hard release-contract audit instead of weakening acceptance. Pushed head `389dba1` has green ordinary Rust CI and Windows Native GUI; five macOS/Linux GUI lanes share one Zig 0.16 strict-analysis compiler SEGV after 23/24 tests pass, and Windows lifecycle independently fails Global MSI same-version repair with exit 2. The release remains queued behind repaired hosted builds, sustained performance, lifecycle matrices, signing/provenance, and public-byte verification (agent: codex)
- 2026-07-21 20:04 — local repair checkpoint passes strict Clippy, 82 Rust tests, 7 immutable v2.0.6 CLI compatibility tests, Native SDK strict validation, and 24/25 optimized native tests. Windows handoff and Linux private-runtime/SBOM fixes are ready for exact-head hosted qualification; no platform or public-release gate has been waived (agent: codex)
- 2026-07-21 19:44 — clean CI run `29880383915` closes the ordinary six-target build failure cluster. Expanded all-platform lifecycle workflows are staged, but final qualification remains correctly queued behind adversarial-review repairs, final MSI/live interaction, two-hour soak, Makira app-embedding license evidence, notarization, attestations, and exact public-byte proof (agent: codex)
- 2026-07-21 23:59 — exact-head ordinary CI and release planning are green at `8aaeef7`; macOS package preflight is green. Managed Windows uninstall still needs the staged ownership-safe cleanup review and rerun, the exact soak is running, and license/signing/public-byte gates remain open; paused for Fable without merging or tagging (agent: codex)
- 2026-07-22 00:04 — the exact soak exited cleanly before duration and produced no JSON, and the operator reported severe minute-old scroll lag. Both now explicitly block #gux and release; no release state changed (agent: codex)
- 2026-07-22 00:27 — expanded the unreleased technical changelog, created its full plain-English companion with all 18 release sections represented, and installed the lockstep changelog rule; this is a truthful partial progress record, not release evidence (agent: codex)
- 2026-07-22 00:32 — committed the changelog, fully reconciled board, and standalone Fable handoff together at local branch HEAD; only the two installer product files remain dirty and the branch was not pushed (agent: codex)
- 2026-07-22 06:20 — bounded post-fix performance sanity PASSED on the final bundle (engine `da4381fb…`): 5-min foreground Processes at 1.65% average CPU of one logical core (budget ≤2%, pre-fix baseline 1.58%), 81.9/224.4 MiB working/private averages within budgets, ~4 MiB end-window deltas (no growth). Scroll sanity also passed with screenshot evidence. Remaining merge gates: consolidated hosted Windows qualification (running) and the push-triggered CI/Release lanes at `f1f1342` (agent: fable)
- 2026-07-22 01:45 — removed the externally failing Claude Code Review workflow per operator direction (commit `970ba9d`, lockstep changelogs): every recent run died before producing findings, leaving a valueless permanent red check. The mention-triggered claude.yml remains. The former backlog item "repair the review workflow" is resolved by removal; in-session independent review stands in (agent: fable)
- 2026-07-22 01:07 — operator approved the Fable release-drive plan: land the reviewed uninstall fix, fix the warmed-state scroll lag through a measured fix ladder, re-prove foreground/hidden budgets on the fixed build, then merge PR #4 to main and drive the release workflow through publication and public-byte verification. Operator waivers recorded: pre-release two-hour soak → Backlog #sok; Makira embedding evidence → ship per explicit operator authorization (purchased, no license file provided). #giu follows as this session's Phase 2 feature release after v3.0.0 is public. Added release subtasks to the board (agent: fable)
