TT;DR: Establish the smallest production-shaped SD-300 native GUI slice without changing the current CLI/TUI. Finish with a pinned, automated CPU/memory GUI packaged into a Corporate MSI that is installed, launched, inspected, and removed on the Alienware host.

## Why
This is a direct operator order and the risk-reduction gate for the larger v3 milestone. The highest-risk seams are Rust/Zig ABI ownership, Native SDK distribution pinning, Windows packaging, and avoiding regressions in the mature v2.0.6 terminal lifecycle. A real MSI slice proves those seams before full UI investment.

Settled decisions: Native SDK 0.5.4 and Zig 0.16.0; compiled native markup with no WebView; Rust `rlib` for the unchanged CLI/TUI plus `cdylib` for the GUI; exact npm tarball and Zig content hash rather than generated absolute `.path`; dark QubeTX design; GUI remains dormant until launched; the app and tray are one process on Windows/macOS; Linux exits on close because the SDK has no Linux tray; Corporate MSI is the first physical installer checkpoint.

## Plan
1. Capture current and immutable v2.0.6 command/output fixtures and record TUI timing/key invariants.
2. Scaffold an owned-build native-rendered app with exact dependency pins and a build-graph audit that rejects local paths.
3. Add a panic-contained, caller-buffer C ABI whose GUI runtime owns its own `SystemSnapshot` and publishes bounded latest-only topics.
4. Implement the first native Overview surface with live CPU/memory values, honest availability, compiled markup, automation, and profiling.
5. Extend the Corporate MSI definition with the GUI, engine, shortcut, and uninstall ownership; install and exercise it on the current Windows host, then remove it and verify no residue.
6. Record the precise hosted feasibility work still required for macOS and all Linux targets before promoting the full-GUI task.

## Impact
Intended: creates the reusable engine/app/package foundation while leaving the TUI control flow and existing public commands unchanged. Possible unintended impact: Cargo crate type changes, Windows DLL search mistakes, ABI mismatch, new build requirements, installer file-table/upgrade conflicts, or CLI snapshot drift. Guard with isolated modules, absolute app-relative loading, exact version checks, golden fixtures, and a real install/uninstall smoke.

## Acceptance
The branch contains a reproducible Native SDK app and Rust engine that build together, render live SD-300 CPU/memory data, pass headless and live automation, and install through a Corporate MSI without changing normal CLI/TUI behavior. The MSI can be removed through the supported owner path with no GUI residue.

## Verification
- [ ] v2.0.6 compatibility fixtures and current Rust tests pass without normal CLI/TUI contract drift — immutable-tag source goldens, live JSON schema checks, noninteractive TUI behavior, native Windows tests, and five non-Windows compile targets pass; published v2 binary PTY replay on hosted targets remains
- [x] Native SDK strict markup/model checks, Zig build, and null-platform tests pass with no absolute SDK dependency
- [x] Live Windows automation proves the GUI is nonblank, updates CPU/memory, has no dispatch errors, and records frame timing
- [x] Corporate MSI installs CLI+GUI, exposes the Start/Search launcher and Installed Apps owner, and launches both surfaces correctly
- [x] `sd300 uninstall` removes the Corporate MSI CLI, GUI, engine, shortcut, registration, settings, and running process without unrelated deletion

## Status
Active. The branch contains the exact-pinned Native SDK 0.5.4 app, dynamically loaded Rust engine, approved Warm Carbon native design, bundled Makira/IBM Plex Mono hierarchy, all nine functional navigation destinations, latest-only bounded projections for the existing Rust collectors, controlled page scroll state, and a real asynchronous driver-rescan action. Windows collector subprocesses remain captured with `CREATE_NO_WINDOW`; subscription-driven Overview, hidden, and Processes profiles avoid collection they do not display. Native strict checks, 24 GUI tests, Linux quit-manifest validation, dependency/path checks, 79 Rust unit plus 7 CLI compatibility tests, focused engine tests, and live Alienware rendering across every section pass. The reported 77% spike was reproduced as one-second full software presentation plus automation instrumentation, not ordinary collector load. The retained renderer now combines multi-region damage, partial Windows upload, position/background-independent glyph coverage, and bounded stable base fragments. On the real 1180x760 SD-300 display list it measured 0.857 ms average/1.365 ms p95 rendering at one hertz with direct/cached byte equality through the release-shaped patched graph; prior collector and host-upload measurements put the component sum below 2% of one logical core. Source cadence is restored to one-second visible presentation. The final clean staged Windows product passes the pinned Native SDK, Rust engine, strict, distribution, and path-leak gates; sustained product performance still must confirm the estimate. A physical Corporate MSI from the earlier staged graph installed from genuine Cargo-owned v2.0.6, launched through `sd300 gui`, and uninstalled with one JSON object and no owned residue. The final MSI now builds after repairing notice-component authoring, no-console self-test reporting, repair-safe verification, and pre-takeover rollback ordering. The current host's first intentionally failing candidate left an orphan Windows Installer registration after rollback, so final clean install/uninstall replay is moving to an ephemeral hosted Windows runner rather than weakening or bypassing Windows secure-repair behavior. Published-v2-binary PTY replay and hosted targets remain open.

## Activity
- 2026-07-20 21:26 — created and moved directly to Active on the requested feature branch (agent: codex)
- 2026-07-20 21:32 — confirmed the immutable v2.0.6 code baseline, passed fmt/Clippy/52 tests, and added the additive CLI compatibility fixture (agent: codex)
- 2026-07-20 22:10 — added the isolated Rust cdylib, versioned/latest-only topic ABI, exact Native SDK dependency lock, compiled native Overview, six-target build wrapper, and distribution/path-leak gate (agent: codex)
- 2026-07-20 22:40 — traced the observed terminal flashes to Windows collector child processes, added a verified no-console subprocess contract, narrowed the GUI to an Overview collection profile, upgraded to Native SDK 0.5.4 for first-class hide/show/quit lifecycle support, and passed strict/null/optimized no-launch gates (agent: codex)
- 2026-07-21 00:04 — locked the operator-approved Warm Carbon render, bundled Makira for primary typography and IBM Plex Mono for technical data, and guarded the SDK's 64-gradient-stop renderer budget at compile time after reproducing and repairing a blank-frame overflow (agent: codex)
- 2026-07-21 00:18 — expanded the ABI consumer into fixed-capacity typed projections for CPU, memory, disks/health, GPUs, interfaces/connections/diagnostics, processes, thermals, and drivers; live Alienware automation showed real data on all nine destinations with zero dispatch errors (agent: codex)
- 2026-07-21 00:18 — passed 12/12 Native SDK tests and strict checks, built ReleaseFast automation, verified scroll reset from a 936.75px Drivers offset, exercised a successful 312-device rescan, and validated the revised memory hierarchy with live 20.8 GiB used of 31.5 GiB (agent: codex)
- 2026-07-21 01:30 — reproduced the reported process spike at 78.54% of one logical core with one-second collection/presentation and isolated it to Native SDK 0.5.4's full software-pixel present rather than the Rust collectors; retained command baselines/memo reduced it to 24.69%, multi-rectangle dirty raster to 8.10%, and partial Windows BGRA conversion/invalidation to 5.24% on Overview (agent: codex)
- 2026-07-21 01:30 — measured Processes separately at 18.40% average/70.82% p95 because one-second CPU sorting changed most row cells; retained one-second values while adding five-sample rank stabilization, a primary top-eight view with on-demand top-16 expansion, explicit per-core CPU semantics, a reusable foreground measurement harness, and 14/14 passing native tests (agent: codex)
- 2026-07-21 03:15 — separated automation overhead from release performance, decoupled one-second collection from eight-second latest-only presentation, added subscription-specific engine profiles and a one-call Windows system-process sampler, and measured production Overview at 0.93% and Processes at 2.63% of one logical core; the latter remains above the 2% release gate (agent: codex)
- 2026-07-21 03:15 — audited the official 0.5.4 tarball against the working renderer and isolated five downstream-modified SDK files; clean-build patching and non-Windows software-renderer qualification remain required before packaging (agent: codex)
- 2026-07-21 03:36 — committed a five-file downstream renderer patch with exact pristine/patched SHA-256 verification, built it through a generated repository-relative staging graph after a fresh npm install, passed path-leak/distribution checks, and measured the clean Processes artifact at 1.92% average CPU, 65 MiB working set, and 173 MiB private commit (agent: codex)
- 2026-07-21 04:08 — built Corporate MSI candidate SHA-256 `87b33aa3d6c944dd5fc4d5b1e2da4fbc901c48fa93510e39f6afc43984358154`, installed it from a genuine crates.io-owned v2.0.6 Cargo state, launched its responsive 54 MiB GUI through the installed CLI, and passed running-GUI `sd300 uninstall --json` with zero process/file/shortcut/registration/owner residue; the original Cargo-owned v2.0.6 install was restored afterward (agent: codex)
- 2026-07-21 04:08 — discovered that a named quit endpoint was required because MSI could remove a still-running GUI image; added an auto-reset-free multi-instance Windows event handoff that posts `WM_QUIT` to the Native SDK UI thread, made lifecycle mutation fail closed if an endpoint is missing, and passed 56 Rust plus 14 Zig tests before physical verification (agent: codex)
- 2026-07-21 05:20 — rebuilt the expanded GUI from a clean generated staging graph; strict dependency/path checks, 20 Zig tests, 6 engine tests, and `--self-test --json` passed against the adjacent dynamic engine with live 22-thread CPU and 31.5-GiB memory discovery (agent: codex)
- 2026-07-21 07:49 — matched the remote `v2.0.6` tag to commit `a81d86cbd8c9617f84072d721e94040cc2a05dd2`, built that archived source without downloading a binary, and captured normalized long/short help, version, four parse-error, capability-ID, report-schema/redaction, and lifecycle-result goldens. Added noninteractive chooser/navigation/help/quit/unit/sort/scroll/cadence/history/async-driver tests; 77 unit plus 7 integration tests, fmt, strict Clippy, and test compilation for Linux GNU x86-64/ARM64, Linux musl x86-64, and macOS x86-64/ARM64 pass. Published v2 binary PTY replay remains a hosted release gate (agent: codex)
- 2026-07-21 08:33 — retained stable base fragments plus position-independent glyph coverage reduced the real SD-300 headless renderer to 1.112 ms average/1.598 ms p95 at one hertz with 1,275 command-memo hits, zero misses after warm-up, and direct/cached byte equality. Restored the product's one-second visible cadence; normal strict checks, 23 GUI tests, 79 Rust unit tests, 7 CLI compatibility tests, and Unix export-permission cross-compilation pass. Clean staged build, physical MSI rebuild, sustained performance, and hosted PTY replay remain open (agent: codex)
- 2026-07-21 08:41 — eliminated the patched-SDK test ambiguity: the canonical wrapper stages the lockfile-verified npm package after applying SHA-256 patch `32ca4fc9a6bf2d2a0933d5bf2b9db5d4b3c3736f27ffcbe4938f76998a350fc1`, writes only a generated repository-relative dependency, and invokes the project-local Native SDK CLI. `$env:SD300_RENDER_BENCH='1'; npm test -- -Doptimize=ReleaseFast` passed 24/24 tests and measured 0.857 ms average/1.365 ms p95 cached rendering with byte equality; clean-cache replay and sustained product gates remain open (agent: codex)
- 2026-07-21 08:47 — replayed the renderer dependency twice from pristine `npm ci --ignore-scripts`, once through each Node/PowerShell patch preparer; exact source hashes and patch receipt matched. Both distribution/path-leak implementations, `npx native check . --strict`, and the SDK-level `zig build test-canvas -Doptimize=ReleaseFast` suite passed. The remaining performance work is product-level sustained/hidden/soak qualification, not a renderer-unit or dependency-reproducibility blocker (agent: codex)
- 2026-07-21 09:40 — passed the final staged Windows release build with 23/24 tests (one expected platform skip), strict dependency/path checks, and live engine self-test. Final MSI linking then exposed and repaired four release defects: multi-file unversioned notice components, success JSON being incorrectly required when Windows Installer provides no stdout, `FileKey` verification failing in maintenance mode, and Cargo takeover occurring before companion qualification. Both final MSI editions now link; a deliberately failed first candidate left a host-only orphan MSI record, so clean lifecycle replay is assigned to an ephemeral hosted runner while the exact v2.0.6 Cargo binary remains restored (agent: codex)
- 2026-07-21 10:02 — completed the feature-branch pre-push gate: `git diff --check`, PowerShell/Node/WiX parsing, actionlint, ShellCheck, Bash syntax, product-version consistency, package notice verification, and developer-path leakage checks all pass. The current source also passes fmt, strict Clippy, 79 Rust unit tests, and 7 CLI compatibility tests; hosted clean-runner lifecycle and six-target qualification are next (agent: codex)
- 2026-07-21 10:12 — moved Makira's commercial source font out of the public Git graph and into two encrypted Actions secrets; a trusted-runner preparer reconstructs and SHA-256-verifies the exact `604208d...` face before compilation. Secret round-trip reconstruction, strict Native SDK checks, and 23/24 final native tests pass; app-embedding/redistribution license evidence remains required before public release (agent: codex)
