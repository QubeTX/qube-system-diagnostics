TT;DR: Qualify and publish v3.0.0 only after the complete GUI and lifecycle matrices pass on all six existing targets and the physical Windows candidate is approved. Probe Native SDK's additional Windows ARM64 and Linux-musl ARM64 toolchains separately; do not advertise them until their complete SD-300 lifecycle is owned and qualified.

## Why
Direct operator order and release closure for the milestone. A green build on one host is not enough for a cross-platform monitoring product.

## Plan
Run Rust/Native SDK quality gates, all hosted native matrices, physical Alienware installer and performance proof, SBOM/provenance generation, immutable release qualification, public-byte verification, Cargo two-update messaging, and website documentation checks.

## Impact
Publishes the new major release to current users. Partial publication or inaccurate platform claims are unacceptable.

## Acceptance
All six existing target and lifecycle gates pass, the public release and crate are consistent, and the website documents install/update/uninstall and verification correctly. Any additional architecture is advertised only after the same installer/update/uninstall and runtime proof.

## Verification
- [x] Hosted and physical qualification matrices pass with archived evidence (passed 2026-07-22 — Windows matrix run 29917852561; producer sweep green on cycles six through nine; physical Alienware acceptance on the released MSI; agent: fable)
- [x] Public crate, release assets, checksums, attestations, stable routers, and website copy agree on v3.0.0 (passed 2026-07-22 — sparse index lists 3.0.0, 59 assets with matching sidecars, `gh attestation verify` exit 0; the stable versionless installer URLs mean existing website commands work unchanged, and a content refresh mentioning the GUI is optional follow-up; agent: fable)
- [x] Fresh install, update, repair, GUI/TUI launch, and uninstall pass against public bytes (passed 2026-07-22 — `cargo install tr300-tui --version 3.0.0` works; public Corporate MSI installed, launched the GUI with live data, exported a snapshot, and uninstalled with zero residue; update/repair routes proven by the hosted matrix on the same bytes; agent: fable)

## Completion
Done 2026-07-22. SD-300 v3.0.0 is public.

## Status
COMPLETED 2026-07-22 — v3.0.0 was published and physically accepted under the evidence recorded above. On 2026-07-23 the operator observed that Windows still showed generic application and tray icons and rejected the shipped ECG-style artwork; that later defect invalidates the historical icon-acceptance subclaim without reopening the immutable release. Corrective work is tracked separately as #n7k.

## Activity
- 2026-07-20 21:26 — created from the approved v3 implementation plan (agent: codex)
- 2026-07-21 07:35 — coordinated all 18 product-version surfaces at 3.0.0, added exact Native SDK/Zig/Rust toolchain records and clean/offline dependency checks, expanded pull-request GUI builds across the five native host lanes plus pinned Alpine musl, and made final qualification fail closed unless source checkout, tag, draft target, artifact checksums, SBOM, and attestations all identify the same candidate. Local workflow structure, distribution pins, path-leak checks, Rust fmt/Clippy, 71 unit tests, 2 CLI compatibility tests, and 7 engine ABI tests pass; hosted and sustained-performance gates remain open (agent: codex)
- 2026-07-21 08:31 — hardened release qualification so an already-public rerun verifies immutable metadata instead of overwriting it, stable post-public installers are checksum-verified before execution, every manifest subject is provenance-verified, explicit tags and existing tags must resolve to the exact Cargo version/source SHA, and all host lanes prove clean-cache plus warmed/offline Native SDK restoration. The local cargo-dist plan contains all six required targets and no generic CLI-only MSI; hosted signing, GTK-free launch, native lifecycle, performance-soak, attestation, and public-byte proof remain open (agent: codex)
- 2026-07-21 18:31 — after 15 hours, performed a hard release-contract audit instead of weakening acceptance. Pushed head `389dba1` has green ordinary Rust CI and Windows Native GUI; five macOS/Linux GUI lanes share one Zig 0.16 strict-analysis compiler SEGV after 23/24 tests pass, and Windows lifecycle independently fails Global MSI same-version repair with exit 2. The release remains queued behind repaired hosted builds, sustained performance, lifecycle matrices, signing/provenance, and public-byte verification (agent: codex)
- 2026-07-21 20:04 — local repair checkpoint passes strict Clippy, 82 Rust tests, 7 immutable v2.0.6 CLI compatibility tests, Native SDK strict validation, and 24/25 optimized native tests. Windows handoff and Linux private-runtime/SBOM fixes are ready for exact-head hosted qualification; no platform or public-release gate has been waived (agent: codex)
- 2026-07-21 19:44 — clean CI run `29880383915` closes the ordinary six-target build failure cluster. Expanded all-platform lifecycle workflows are staged, but final qualification remains correctly queued behind adversarial-review repairs, final MSI/live interaction, two-hour soak, Makira app-embedding license evidence, notarization, attestations, and exact public-byte proof (agent: codex)
- 2026-07-21 23:59 — exact-head ordinary CI and release planning are green at `8aaeef7`; macOS package preflight is green. Managed Windows uninstall still needs the staged ownership-safe cleanup review and rerun, the exact soak is running, and license/signing/public-byte gates remain open; paused for Fable without merging or tagging (agent: codex)
- 2026-07-22 00:04 — the exact soak exited cleanly before duration and produced no JSON, and the operator reported severe minute-old scroll lag. Both now explicitly block #gux and release; no release state changed (agent: codex)
- 2026-07-22 00:27 — expanded the unreleased technical changelog, created its full plain-English companion with all 18 release sections represented, and installed the lockstep changelog rule; this is a truthful partial progress record, not release evidence (agent: codex)
- 2026-07-22 00:32 — committed the changelog, fully reconciled board, and standalone Fable handoff together at local branch HEAD; only the two installer product files remain dirty and the branch was not pushed (agent: codex)
- 2026-07-22 18:10 — **PUBLIC-BYTE ACCEPTANCE PASSED on the released artifacts.** `cargo install tr300-tui --version 3.0.0 --locked` builds and installs a working `sd300 3.0.0` from crates.io with the `gui` command present. Downloaded the public Corporate MSI and GUI archive: both checksums match their sidecars, and `gh attestation verify` on the MSI exits 0 against the repository. Physically installed the public MSI (exit 0), launched the GUI through the installed CLI — live Alienware data, all nine sections, screenshot retained — took a redacted snapshot (25 capabilities), confirmed the Start Menu entry, then `sd300 uninstall --json` returned one success object and removed the payload, process, and shortcut with no residue. Two cosmetic/CI defects found for a patch: the sidebar hardcodes "v3.0.0 QUALIFICATION", and the post-publication crate poll uses crates.io's web API, which now 403s unidentified clients (agent: fable)
- 2026-07-22 17:35 — **SD-300 v3.0.0 IS PUBLISHED.** Cycle nine carried the full chain: Release green, all three producers green, and the publish gate ran its complete sequence — draft Linux lifecycle and immutable Cargo migration, SPDX SBOM, source/checksum sealing, all three attestations (provenance, SBOM, exact source identity), `cargo publish`, and promotion of the draft to latest. Public at 15:02:41Z with 59 assets; the crate is live (sparse index lists 3.0.0). The gate's one red step is the post-publication convergence poll, which curls crates.io's web API — that API now returns 403 to unidentified clients under their data-access policy, so the check is broken independently of the release. Fixing the check and then completing the public-byte proof (agent: fable)
- 2026-07-22 16:45 — cycle seven: all three producers green again (56 assets) and the publish gate advanced past the placeholder guard into the path-leak scan, which false-positived on cargo-dist's own generated comment documenting PATH semantics with the literal example `/home/myuser/.myapp`. Exempted that exact upstream boilerplate line (proved locally that a real `/home/<user>/` leak still fails the scan) and re-triggered as cycle eight (`9a7871f`). Publish steps still untried: SBOM, three attestations, crate publish, latest promotion, public-byte proof (agent: fable)
- 2026-07-22 16:05 — cycle six: **all three producers GREEN on one commit** (windows/linux/macos) for the first time in v3 history, draft at 56 assets, and the publish gate advanced into full candidate qualification — downloading and checksum-verifying every updater-facing artifact (all OK) before failing on a second copy of the same placeholder guard already fixed in the host job. Fixed in release-qualify.yml (`9afef1b`); fresh single-SHA cycle re-triggered. Remaining publish steps after that guard: SBOM, provenance attestations, crate publish, latest promotion, public-byte proof (agent: fable)
- 2026-07-22 15:20 — cycle five reached the publish gate: Release green, draft created, **Windows and Linux producers both fully GREEN** and their assets attached (draft reached 52 assets), and the publish gate ran correctly in waiting mode ("Qualification is waiting for sd300-macos-universal.pkg…"). Only macOS failed, on the same legacy `.crates.toml` fixture gap already fixed for Windows — now fixed for macOS too. Because the fix changed a workflow after the draft was cut, the draft's target-SHA identity check would reject a partial re-run, so the draft was deleted and a fresh single-SHA cycle re-triggered rather than weakening that check (agent: fable)
- 2026-07-22 14:30 — the draft-identity fix worked: Release green again, draft v3.0.0 recreated, and all three native producers RAN for the first time. macOS signing AND notarization both succeeded ("accepted / source=Notarized Developer ID") before failing on a relative archive path inside a changed-directory subshell; Linux failed because GitHub serves draft assets only through the API, so the nested cargo-dist installer's public download URL 404s even with a token. Both fixed (`1d672ac`): asset directories now feed the installer via `file://`, and the macOS output directory is absolute. Windows producer still running (agent: fable)
- 2026-07-22 13:45 — third attempt went FULLY GREEN through the Release workflow (all six targets, host, announce; draft v3.0.0 with 32 assets) — then all three chained producers failed the same way: their hardened identity checks unconditionally fetch `refs/tags/v3.0.0`, but GitHub only materializes a release tag at publish, so a true unpublished draft can never pass (the hardening had only ever run in qualification-only mode). Fixed all six sites across four workflows to verify the tag when the ref exists and rely on the already-verified draft target commitish otherwise; deleted the stale unpublished draft per the workflow's own fail-forward contract and re-triggered (`822ad86`) (agent: fable)
- 2026-07-22 13:15 — second release attempt: source-check green, ALL SIX cargo-dist targets built, global artifacts built; the `host` job then failed on a placeholder-guard false positive — the v3 managed wrapper legitimately carries `@SD300_RUNTIME@`/`@SD300_GUI@` in its own runtime-side sed commands, and the blanket `@SD300_` grep flagged them. Guard narrowed to the two build-time placeholders actually rendered (`2d1fbfb`); Release re-triggered with the dual-frontend docs riding along (agent: fable)
- 2026-07-22 12:50 — PR #4 merged to main (`4e17c41`). The first release attempt failed in source-check: inserting the worker error-report helper had consumed `run_windows_uninstall_worker`'s `#[cfg(windows)]` gate, so the Windows-only body compiled on Linux (a cross-target check gap on that one commit — recorded for the post-mortem). Fixed forward on main (`7e80d74`) after all five non-Windows target checks, clippy, and the full suite passed locally; Release re-triggered and running (agent: fable)
- 2026-07-22 06:20 — bounded post-fix performance sanity PASSED on the final bundle (engine `da4381fb…`): 5-min foreground Processes at 1.65% average CPU of one logical core (budget ≤2%, pre-fix baseline 1.58%), 81.9/224.4 MiB working/private averages within budgets, ~4 MiB end-window deltas (no growth). Scroll sanity also passed with screenshot evidence. Remaining merge gates: consolidated hosted Windows qualification (running) and the push-triggered CI/Release lanes at `f1f1342` (agent: fable)
- 2026-07-22 01:45 — removed the externally failing Claude Code Review workflow per operator direction (commit `970ba9d`, lockstep changelogs): every recent run died before producing findings, leaving a valueless permanent red check. The mention-triggered claude.yml remains. The former backlog item "repair the review workflow" is resolved by removal; in-session independent review stands in (agent: fable)
- 2026-07-22 01:07 — operator approved the Fable release-drive plan: land the reviewed uninstall fix, fix the warmed-state scroll lag through a measured fix ladder, re-prove foreground/hidden budgets on the fixed build, then merge PR #4 to main and drive the release workflow through publication and public-byte verification. Operator waivers recorded: pre-release two-hour soak → Backlog #sok; Makira embedding evidence → ship per explicit operator authorization (purchased, no license file provided). #giu follows as this session's Phase 2 feature release after v3.0.0 is public. Added release subtasks to the board (agent: fable)
- 2026-07-23 01:09 — operator's installed Windows observation invalidated the prior icon-acceptance subclaim: the updater installed the PNG, but the EXE lacked an embedded icon and the Win32 host treated the PNG as an ICO before falling back to generic. The v3 release remains completed; corrective artwork and delivery work moved to #n7k (agent: codex)
