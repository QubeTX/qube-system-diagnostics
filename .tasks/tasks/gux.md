TT;DR: Build the complete Native SDK GUI after the vertical slice proves the architecture. Every current SD-300 diagnostic and both audience modes must be represented with richer native charts, tables, detail, and honest availability.

## Why
Direct operator order. The GUI should make the existing diagnostics more legible without replacing or altering the TUI.

## Plan
Translate the QubeTX design tokens into Native SDK themes; inventory all existing TUI/report fields; implement Overview, CPU, Memory, Disk, GPU, Network, Processes, Thermals, and Drivers; add virtualized lists, charts, details, exports, GUI-only settings, keyboard/accessibility behavior, and supported Windows/macOS tray integration.

## Impact
Adds the primary user-facing desktop experience. Risks include parity omissions, excessive retained nodes, decorative resource use, and platform capability overclaims.

## Acceptance
The parity inventory is complete in User and Technician modes, all platform gaps are honest, and the strict CPU/memory/frame/input budgets pass.

## Verification
- [x] Native SDK model/markup strict checks and GUI unit tests pass
- [ ] Automation covers every section, both modes, keyboard paths, scaling, exports, and unavailable states
- [ ] Scrollable sections remain responsive after at least one minute open and meet the <=50 ms input-p95 budget outside scans
- [ ] Foreground and hidden/tray performance budgets pass on the fixed release build
- [~] Two-hour soak passes before release (waived 2026-07-22 — operator directed release focus; the soak moves to Backlog #sok against released bytes; agent: fable)

## Status
Active. The approved Warm Carbon design is running in the real Native SDK renderer with bundled Makira primary and IBM Plex Mono technical typography. All nine navigation destinations render bounded live data from the shared Rust collectors. User and Technician modes now change the information hierarchy; process sorting/search/warm-up state, warning shown/total counts, active-topic freshness/provenance, network adapter/connection filtering, display active state, battery/power detail, disk capacity/activity/reliability separation, physical-drive health, GPU and thermal availability, driver filtering/service detail, GUI help, GUI-only persisted settings, manual driver scan, and redacted snapshot/capabilities exports are implemented. Fixed 60-sample histories now cover CPU, memory, swap, network down/up, disk read/write, GPU, and temperature. The app restores its last section and collection profile, publishes a richer tray summary, and presents bounded latest-only data every second. The clean release-shaped Windows build passes strict checks and 31 GUI tests with one expected platform skip. A persistent Windows DIB lowered presentation cost; the first 15-minute one-second Processes run reached 2.09% of one logical core with passing memory, narrowly missing the 2% gate. Native stage profiling showed the remaining avoidable work was SDK 0.5.4's development-default per-event trace serialization. Release wrappers now compile that tracing off while retaining panic capture and explicit self-tests; later exact foreground and hidden averages pass. Physical keyboard/scaling/export and restart-sensitive tray/autostart behavior pass; internal screen-reader control exposure is unavailable, severe scroll lag after roughly one minute is open, and the final two-hour soak exited early and is invalid.

Current checkpoint: the exact footer-free, trace-off bundle passes both required sustained Windows samples while preserving one-second collection/presentation: the 15-minute foreground Processes run averaged 1.58% of one logical core with 84.78 MiB working set and 227.2 MiB private memory, and the 30-minute hidden run averaged 0.18% with 64.98 MiB working set and 206.65 MiB private memory. The Windows process sampler now commits its CPU baseline only after a complete inventory query, preventing failed fast-path fallback from manufacturing the reported one-frame 77% value. The final rebuilt two-hour soak, frame/input evidence, physical keyboard/scaling/accessibility/export, and restart-sensitive tray/autostart exercise remain open.

Handoff checkpoint: physical Windows acceptance now covers all nine destinations, both modes, keyboard navigation, maximized scaling, redacted export, singleton focus, hidden startup, repeat close-to-tray, launch-at-login add/remove, restored defaults, and ordinary close. Native SDK exposes the named canvas but not internal controls to Windows/Linux screen readers, so the unchanged TUI remains the documented accessibility fallback. The exact final app `d83de90b...7acaa2` began the two-hour soak as PID 4260 and reached 31.06 CPU seconds, 86.41 MiB working set, and 226.18 MiB private memory, but then exited with code 0 at 00:03. The harness wrote the error only to `target/performance-final-soak-20260721-233810.stderr.log`; its JSON is empty. Do not mark automation or performance complete until the exit source, scroll lag, replacement soak, and explicit frame/input evidence are resolved.

Operator-observed blocker at handoff: after the installed/end-user app has remained open for roughly one minute, scrolling on sections with scrollable content becomes severely laggy. This was not captured by the foreground/hidden CPU averages and is not cleared by the running noninteractive soak. Treat it as a release-blocking interaction regression. Reproduce on the exact final bundle with a scrollable page and at least a one-minute warm-up, record input-to-present latency and queue/backlog behavior, isolate whether histories/timers/model updates accumulate work, and retest without Codex Computer Use/UI Automation attached so observer overhead is not mistaken for product cost.

## Activity
- 2026-07-22 04:20 — operator physical test of the fresh bundle (engine `ee368706…`) REJECTED the scroll fix: one wheel click now scrolls the entire range in the click's direction over ~2 seconds with no intermediate positions, and intermittent lag persists on Network/Processes. Symptom shape (single event applied repeatedly until boundary) points at the host-input-record ↔ runtime per-frame consumption seam interacting with the coalescing change. Investigation dispatched; subtask remains open (agent: fable)
- 2026-07-22 03:50 — pushed the scroll fix and lifecycle fixes as `f1e0d08` (Windows host wheel-burst coalescing through the pinned patch, all hashes lockstep) and `1231d1c` (session tray presence + minimized cadence). Combined validation: 35/35 native tests, strict checks, both preparers reproduce from pristine, distribution verifiers green, staged release-shaped build compiles the change. Remaining gates for this subtask: operator physical warmed-scroll acceptance on the fresh bundle, then bounded foreground/hidden perf re-proof (agent: fable)
- 2026-07-22 03:25 — both review-surfaced lifecycle bugs are fixed and Fable-reviewed (uncommitted, riding the consolidation commit): the quit decision now consults a startup-effective `tray_session_active` (mid-session tray toggles can no longer strand a hidden icon-less process or kill a live icon), and cadence now derives from policy-hidden rather than raw visibility (a minimized window keeps 1 s sampling and the foreground engine profile, so taskbar restore is instantly fresh; tray-hidden stays 30 s). Full tray_enabled read-site audit: 9 sites, only the quit site needed the session value. 35/35 native tests including two new regression tests; strict check and distribution locks green (agent: fable)
- 2026-07-22 02:40 — landed the warmed-state scroll benchmark (commit `fba9e85`, +515 test-only lines, 33/33 tests) and ADR 0002. Measured root cause: every scroll frame is a full-viewport union repaint (rects=1, ratio 1.0, memo defeated by translation) at 15.8–17.0 ms avg / 18–22 ms p95 — from launch, warm or cold; steady ticks cost 0.27–1.7 ms, with warmed charts (1.7 ms Network tick) staying sparse. The ~60 s onset is queue collision: filled histories turn free idle ticks into mandatory per-second chart repaints that contend with budget-saturating scroll frames on the shared single-threaded update queue. Fix in flight: runtime scroll coalescing + input-before-present through the pinned patch; blit-scroll held in reserve. Independent GUI review also returned two SHOULD-FIX lifecycle bugs now being fixed: mid-session tray toggle can strand a hidden no-tray process, and un-minimize can present up to 30 s stale data (agent: fable)
- 2026-07-20 21:26 — created from the approved v3 implementation plan (agent: codex)
- 2026-07-21 00:04 — selected and implemented the operator-approved Warm Carbon composition from the generated mockup; the actual Native SDK screenshot was accepted as the visual baseline (agent: codex)
- 2026-07-21 00:18 — made Overview, CPU, Memory, Disk, GPU, Network, Processes, Thermals, and Drivers real destinations backed by fixed-capacity topic projections; passed 12 native tests/strict checks and live-rendered all sections with zero dispatch errors (agent: codex)
- 2026-07-21 01:30 — documented a research-backed status/evidence/cause/inventory hierarchy for every page; kept one-second collection and foreground presentation while optimizing the retained renderer, clarified process CPU as percent of one logical core, and added stable ranked-row behavior plus primary/on-demand density (agent: codex)
- 2026-07-21 03:15 — preserved one-second engine sampling but moved ordinary visible presentation to the latest bounded sample every eight seconds, split per-page collector subscriptions, and stabilized process row identity over 30 samples; production Overview now passes the CPU budget while Processes remains 0.63 percentage points over target (agent: codex)
- 2026-07-21 03:36 — the reproducible cold-built Processes artifact passed the short foreground average CPU gate at 1.92% of one logical core while preserving one-second sampling; the mandatory 15-minute/hidden/two-hour qualification remains open (agent: codex)
- 2026-07-21 05:20 — added meaningful User/Technician information hierarchies, sortable process projections, warnings/capabilities/provenance, additional hardware details, persisted GUI-only settings, and asynchronous redacted snapshot/capabilities exports; a fresh staged Windows build passed strict distribution checks, 20 Zig tests, 6 engine tests, and live CPU/memory self-test (agent: codex)
- 2026-07-21 06:58 — isolated the Processes spike with Native SDK frame instrumentation: Rust process collection averaged 12.37 ms/sample and Windows host pixel upload took 0.6–1.0 ms, while the patched software raster path spent 136–215 ms replaying the command list across eight dirty rectangles. A single union repaint regressed the one-minute Processes run to 7.99% of one logical core, proving that neither extreme is acceptable; multi-region one-pass damage rendering is under implementation. The 77% report remains a real release blocker until the packaged sustained gate passes (agent: codex)
- 2026-07-21 08:18 — completed a field-by-field parity audit against every TUI section and collector surface. The audit confirms real coverage for all nine destinations while keeping disk activity/reliability detail, non-CPU histories, driver services, table filtering/paging, topic freshness/provenance, and GUI interaction help explicitly open rather than treating exports or bounded rows as silent parity (agent: codex)
- 2026-07-21 08:33 — implemented the audited parity gaps: active-topic freshness/provenance rail; memory/swap, network, disk-I/O, GPU, and temperature histories; disk throughput/queue/latency/error availability; hypervisor/removable/display-active/media-state details; CPU/memory cause jumps; process/connection/driver search and filtering; process warm-up, shown/total semantics, driver services, and interaction help. Native strict checks and 23 tests pass. Restored one-second visible presentation after the retained glyph/base-fragment renderer measured 1.112 ms average and 1.598 ms p95 with direct/cached byte equality; staged-product and sustained gates remain open (agent: codex)
- 2026-07-21 08:41 — added a repository-owned release-shaped Native SDK test wrapper so the immutable URL/hash manifest remains clean while `native test` consumes the hash-verified downstream renderer through a generated relative dependency. `$env:SD300_RENDER_BENCH='1'; npm test -- -Doptimize=ReleaseFast` passed 24/24 tests with direct/cached byte equality, 0.857 ms average/1.365 ms p95 cached rendering, 1,275 command-memo hits, and zero warmed command misses (agent: codex)
- 2026-07-21 14:47 — the clean final Windows app passed strict/native/package self-tests, then a 60-second release Processes measurement averaged 2.39% of one logical core. Working-set and private-memory budgets pass; the CPU gate remains release-blocking and the task record no longer relies on component-sum estimates (agent: codex)
- 2026-07-21 16:02 — pinned and clean-built a persistent Windows DIB presentation surface, improving the 15-minute Processes workload to 2.09% while preserving one-second collection/presentation. Native SDK frame profiling then exposed release-default `trace=events` as avoidable high-frequency JSON/stdout work; both release wrappers now pass `-Dtrace=off`. Two repeat samples pass at 1.77% and 1.82%, with the owned clean-build sustained, hidden, and soak gates still open (agent: codex)
- 2026-07-21 18:31 — the clean trace-off 15-minute Processes artifact averaged 2.24%, split approximately 1.15% window and 1.09% worker, so the strict CPU gate remains open. The subsequent typed process ABI, direct envelope serialization, reusable sampler/top-N work, and repeating timer compile into exact engine SHA-256 `74b7c3e1...` and pass 24/25 optimized Native tests; final combined sustained qualification remains pending (agent: codex)
- 2026-07-21 20:04 — refreshed live Windows process CPU from the same aggregate sampler and updated memory counters in place, bringing the first 60-second Processes check to 1.95% of one logical core with 78.69 MiB average working set and 222.21 MiB private commit. Removed the redundant bottom collection/refresh status rail while retaining export feedback in context. Strict Clippy, 82 Rust tests, 7 v2.0.6 CLI compatibility tests, Native SDK strict checks, and 24/25 optimized native tests pass; the required sustained/hidden/soak run remains open (agent: codex)
- 2026-07-21 20:10 — produced the complete footer-free Windows ReleaseFast bundle through the distribution-locked wrapper. It passes 24/25 native tests with one expected skip, path/dependency checks, and live adjacent-engine self-test; engine SHA-256 is `1c40c9865a73213d83e05cb09df8e57658eb934d7743cafe25d47e1ebc4e9865` (agent: codex)
- 2026-07-21 18:49 — passed the release-blocking 15-minute foreground Processes test on the exact footer-free artifact: 1.58% average CPU of one logical core, 84.78/90.36 MiB average/maximum working set, and 227.2/232.74 MiB average/maximum private memory. All 3,363 samples remained live and the app exited through its authenticated lifecycle endpoint (agent: codex)
- 2026-07-21 19:19 — passed the 30-minute hidden/tray gate on the same release-shaped app across 3,465 samples: 0.18% average CPU of one logical core, 64.98/66.84 MiB average/maximum working set, and 206.65/208.81 MiB average/maximum private memory. Added first-versus-last memory-window deltas to the soak harness, fixed transactional Windows process CPU sampling, hardened startup/settings ownership, and passed 88 Rust unit plus 7 immutable-v2 compatibility tests and strict Clippy (agent: codex)
- 2026-07-21 19:44 — completed explicit unavailable/denied/unsupported observation presentation and source shown/total semantics across memory modules, adapters, displays, GPU, thermals, battery, disk health/reliability, drivers, services, warnings, and capabilities. CPU frequency zero is now treated as not reported. Native strict checks and 27/28 tests pass; the top live indicator still needs the adversarial-review stale-state correction before final interaction qualification (agent: codex)
- 2026-07-21 22:35 — isolated a false performance regression to the still-running Codex Computer Use/UI Automation observer: the unchanged release GUI fell from a contaminated 5.39% to 1.61% of one logical core after the supported observer shutdown, split 0.67% window/0.94% engine. Physical native launch, all nine destinations, singleton focus, maximized scaling, redacted export, and normal close pass. Windows/Linux system screen readers expose only the named Native SDK canvas, so the documented TUI fallback remains an explicit platform limitation. Regenerating the model contract exposed and removed dead footer bindings; strict validation and 30/31 tests now pass with one expected platform skip. Final automation/tray/startup and two-hour soak evidence remain open (agent: codex)
- 2026-07-21 23:38 — converged the SDK-fixed host close policy with runtime tray defaults, startup intent, and a UI-thread Windows singleton message route. Exact final app `d83de90b...7acaa2` physically passes hidden startup, same-PID reopen with resumed one-second samples, repeat close-to-tray, launch-at-login add/remove, restored defaults, and ordinary close/exit. Strict model validation and 31/32 native tests pass; the observer is closed and the exact two-hour Processes soak is running (agent: codex)
- 2026-07-21 23:59 — exact-head hosted Native GUI builds are green on all six contract targets. Paused for Fable while PID 4260 continues the isolated two-hour soak; frame/input evidence and final soak parsing remain the exact next GUI gates (agent: codex)
- 2026-07-21 23:59 — operator reported severe scrolling lag on scrollable sections after the end-user app has been open for about one minute. Added an explicit subtask and <=50 ms input-p95 verification gate; this interaction regression remains release-blocking even if the noninteractive soak passes (agent: codex)
- 2026-07-22 00:04 — the exact soak app exited normally before duration and invalidated the run; stderr reports `SD-300 GUI exited during measurement with code 0` at harness line 257, with no Application Error event. Capture the graceful-exit source before replacement qualification (agent: codex)
- 2026-07-22 00:27 — paused for Fable after recording the operator's end-user scroll-lag report in both changelogs and the handoff; no controlled reproduction or latency trace exists yet, so the subtask and input/frame gates remain open (agent: codex)
- 2026-07-22 01:07 — attributed the early clean soak exit: the harness cannot signal quit mid-run (its endpoint block is guarded by `-not $process.HasExited`), no watchdog/self-exit exists, and Windows' compile-time `close_policy = .hide` converts ANY window-close into hidden state that the next 1-second tick turns into graceful quit when tray is off (`shouldQuitForHiddenWindow`, main.zig:388-397/568-574/1406). The operator confirmed probably closing the window around 00:03. Exit code 0 at 00:03:52 is operator interaction, not a product defect; subtask checked. Unattended reruns must quarantine the machine (agent: fable)
- 2026-07-22 01:07 — converged scroll-lag root cause across an Opus design agent and independent Fable code verification: shift-left `pushHistory` fills all 60 samples after ~60 s, warmed `chartDataEqual` invalidation plus table churn exceeds the 8-dirty-rect cap (serialization.zig:1039) or trips the 0.75 union fallback, escalating ticks into full-view/N×M command replays that starve scroll input on the shared single-threaded update loop. Fix ladder approved: (1) extend SD300_RENDER_BENCH to warmed-state measurement, (2) app-side visible-page data-tick scoping in main.zig, (3) if needed, runtime scroll-coalescing/input-before-present via the pinned SDK patch with lockstep hash updates. Raising the rect cap is rejected. Per operator, the pre-release soak is waived to Backlog #sok; foreground/hidden budgets will be re-proven on the fixed build (agent: fable)
