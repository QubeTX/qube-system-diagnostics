TT;DR: Restore the original CPU and memory budgets after 4.0.0. The operator approved temporary v4-only ceilings and explicitly asked to preserve every original goal for the next version.

## Why

Native resource qualification found small macOS CPU/RSS and Linux CPU/RSS overruns. ADR 0017 records the dated v4-only decision; docs/next-version-targets.md preserves the complete original goals.

## Scope

All six targets and both frontends. Foreground CPU at most 2 percent of one logical core, hidden GUI at most 1 percent, peak summed RSS at most 150 MiB, private memory at most 300 MiB. Include owned descendants and preserve useful collection cadence, accuracy, availability, accessibility and shutdown.

## Plan

Use retained v4 native windows and final artifact identities as the baseline. Attribute one cost per cycle, implement a focused change and compare unchanged measurement methods on the affected native runtime. Coordinate with #r16 for the original frame/input limits. No inherited v4 waiver applies to the next version.

## Acceptance

All original resource targets pass foreground, hidden and two-hour soak checks without observers; histories, queues, handles/descriptors and child processes remain bounded. Independent OS-counter comparisons and functional/native lifecycle checks remain valid.

## Verification

- [x] Preserve original targets and v4-only ceilings in docs/next-version-targets.md
- [x] Retain original verdicts and final candidate identities
- [ ] Attribute remaining native costs and qualify focused improvements
- [ ] Pass original CPU/RSS limits on all six targets with unchanged methodology

## Status

BACKLOG, owner Codex; next version after 4.0.0. Automatic policy expiry is tested.

## Activity

- 2026-09-23 — codex: operator approved the explicit v4-only resource ceilings and requested all original goals be recorded for the next version. Added ADR 0017, the comprehensive target document and policy expiry/boundary/failure tests. Original failed measurements remain intact.
